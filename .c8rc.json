{
  "reporter": ["text-summary", "text"],
  "include": ["apps/api/dist/**", "packages/shared/dist/**"],
  "exclude": [
    "**/*.map",
    "**/node_modules/**",
    "**/dist/**/fake.provider.js",
    "**/dist/agents/diagnostics.agent.js"
  ],
  "all": false,
  "//check-coverage": "Visibility-first (matches ci.yml): the unit run prints a coverage summary but does NOT gate the build — most of the app is behaviour-covered by the integration + e2e jobs, which ARE the enforced correctness gates. Re-enable check-coverage once coverage is scoped to unit-owned pure logic; the numbers below are the aspirational target.",
  "check-coverage": false,
  "statements": 95,
  "lines": 95,
  "functions": 95,
  "branches": 85
}
