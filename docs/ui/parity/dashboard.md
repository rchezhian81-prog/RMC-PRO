# Parity matrix — Dashboard (Owner Command Centre) · U2

**Route:** `/app/dashboard` — `apps/web/src/app/app/dashboard/page.tsx`
**Change type:** presentation only — re-laid on the U1 command surfaces. No data,
API, route, permission, or control change. Every KPI keeps its exact link.

**Screenshots:** command-centre layout captured in the `/ui-kit` harness
(desktop/tablet/mobile × light/dark). The full authenticated-route pixel baseline
(`dashboard-*-v2`) must be regenerated by the visual stack in CI — Postgres is not
available in this dev container (see Notes).

| Live behavior / control | Existing implementation | New visual location | Behavior preserved | Evidence |
|---|---|---|---|---|
| Fetch summary + funnel | `Promise.all([dashboardApi.summary(), dashboardApi.funnel()])` in `useEffect` | unchanged | ✅ identical call, identical state | code diff (logic block byte-identical) |
| Loading state | `<Loading label="Loading dashboard…" />` early-return | unchanged | ✅ same guard | code diff |
| Error state | `<ErrorState message={error} />` early-return | unchanged | ✅ same guard | code diff |
| Header title + subtitle | inline `<h1>` + `<p>` | `<CommandBar title subtitle>` (same strings) | ✅ text unchanged; presentation only | harness shot |
| 11 KPI tiles (link + tone + value) | bespoke `grid` of `<StatCard href tone>` | `<SummaryStrip>` of the **same** `<StatCard>`s | ✅ same hrefs, tones, values, order | code diff (tiles array untouched) |
| Rule-based alerts | `<AlertsCard />` | unchanged (same position) | ✅ component untouched | code diff |
| AI insights (hide when AI off) | `<InsightsCard />` | unchanged | ✅ component untouched | code diff |
| Order-to-cash funnel (7 steps) | `<Card title>` + step tiles | `<Surface variant="command">` + elevated step tiles | ✅ same steps, labels, values | code diff (funnelSteps untouched) |

## Functional regression checklist

- [x] All original routes still resolve — 11 tile `href`s unchanged (`/app/orders`, `/app/credit-holds`, … `/app/devices`)
- [x] All original links work — same `StatCard href`, same handlers (none added)
- [x] All original fields present — no form on this route
- [x] Permissions unchanged — dashboard is not perm-gated here; API `me()`-driven data unchanged
- [x] Tenant / plant boundaries unchanged — no data-scope change
- [x] API requests unchanged — same `dashboardApi.summary()` + `.funnel()`, same contract
- [x] Offline / sync indicators — n/a on this route (Devices tile links out, unchanged)
- [x] Audit events — none triggered by this route
- [x] Import / export — n/a
- [x] Keyboard workflow — links remain tab-focusable; no dialog on this route
- [x] Automation selectors — no `data-testid` removed; StatCard/link structure preserved
- [x] Unit / integration tests green — shared/api unit suite unaffected (web-only change)
- [ ] Visual-regression diff reviewed — **pending CI** (authenticated-route baseline needs Postgres; harness preview attached)
- [x] No performance regression — same data path; no new network calls; shared JS unchanged (102 kB)

## Notes / intentional visual changes

- Header now sits on a **command bar** surface; funnel on a **command surface** with
  elevated step tiles; KPI tiles in the shared **summary strip** (denser
  `minmax(168px)` vs the prior `190px`). These are the intended U2 elevation.
- **Postgres unavailable** in this dev container, so the authenticated `/app/dashboard`
  pixel baseline (`dashboard-light-v2` / `-dark-v2` × 4 viewports) could not be
  regenerated here. The layout is evidenced by the `/ui-kit` harness preview (same
  U1 primitives); re-baseline on the full visual stack (CI) before merge.
