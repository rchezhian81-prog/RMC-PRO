# SummaryStrip
The KPI row of the owner dashboard: an auto-fit grid of `StatCard`s with a 168px minimum tile and 12px gap (212px and 14px under v2, so the big rupee figures fit).

- The consumer provides the tiles; keep the order stable across days so an owner finds a figure by position. Lead with what needs attention (credit holds, negative stock, overdue) and colour only those with a tone.
